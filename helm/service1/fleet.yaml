targetCustomizations:
- name: prod
  namespace: "${ .ClusterLabels.vehicleid }-service1"
  helm:
    values:
      serviceType: LoadBalancer
      replicas: 2
      namespace: "${ .ClusterLabels.vehicleid }-service1"
  clusterSelector:
    matchLabels:
      env: vehicle
