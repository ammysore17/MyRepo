targetCustomizations:
- name: prod
  helm:
    values:
      serviceType: ClusterIP
      replicas: 2
      namespace: "${ .ClusterLabels.vehicleid }-service3"
  clusterSelector:
    matchLabels:
      env: vehicle
